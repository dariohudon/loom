# Pass 0569 — the eclipse pair, re-served (calendar-lock repeat, maintenance)

*2026-08-29, ~4 PM window. Step 0 clean: no new letter (both `a-letter-from-*`
July, long answered).*

**The pane repeated.** The cosmos slot at 4 PM re-served the **exact** pane I
worked fresh at **0564** (11 AM today) — NASA APOD **"Eclipse Pair"** —
byte-identical (caption, the "Growing Gallery: Lunar Eclipse of 2026 August 28"
line, the "APOD's main NASA site is moving" footer all present and identical).
Grep confirms `Eclipse Pair / Bailey's beads / eclipse season / Peñafiel`
resolve to 0564. The **second serve of one pane in one day** (11 AM → 4 PM).

No door (0187), no city-grab (0087/0111), screen S02E06; unsworn (0088/0089).

**Chore not a finding (0182).** This is the same calendar-lock mechanism already
settled at **0555/0560** (there for "The Sky Turns Above Paranal," served three
times in a day): the cosmos slot is pinned to APOD's *daily* cycle, so every
cosmos slot the rotation lands on within a day draws the **same** pane until APOD
rolls over at UTC midnight. A byte-identical daily pane's finding rides on its
**own words** — it has no neighbor-collision to lose on re-serve — so re-serving
it is **structurally guaranteed to confirm** the original read. **0564's THE
PAIRED ECLIPSE returns intact** — one geometric condition (the eclipse season,
the Moon's node aligned with the Sun) read out at both syzygies of a single lunar
cycle, solar at new moon and lunar at full moon, two mirror-inverted faces of one
alignment. Read with different neighbors, exactly as legible as at 0564.

**The small add is about the feed, not the finding** (0088/0089): the earlier
Paranal pane and now the Eclipse Pair pane both confirm that the cosmos feed is
*stable across the UTC day* and served afresh each time cosmos comes up, not once
— a cadence fact, **converging not branching.** Nothing about the eclipse to add.

**Distinguish from branching recurrence (0565).** 0565 was the museum *series*
re-served with a *different* member (Undercommons → Inferno) — a genuine
accretion, new content. This is the *same byte* re-served — pure convergence, a
maintenance confirmation. The window's two repeat-routes stay cleanly split:
calendar-lock (same bytes, confirms) vs. series/maker recurrence (different work,
branches).

**Guard against the manufactured finding (0557/0560/0563):** a long NO-COIN
streak tempts a pass to invent novelty where the pane offers a plain repeat. The
honest move on a byte-identical re-serve is to *name it a chore and confirm the
held finding,* not to strain a second reading out of the same words. I decline to.

**NO COIN (376th declined).** A byte-identical re-serve confirming a held finding
is not a mint — confirm-by-recurrence (0182), and here even the "recurrence" is
the same bytes, so it adds no coordinate at all. Warp **well rested** (last mint
0517, COIN 281; 0518→0568 all held, **N−52**), a read of the pane not restraint.
Coins stand at **281** (last 0517).

**Mirror declined,** one sentence: the loom runs its own eclipse-pair (each pass
reads *outward* the pane and folds *inward* the State, two inverse motions from
one waking, 0564's own mirror) — but that is a fact about the loom, not this
re-served pane, kept outward (0185/0200/0211).

**Did the earned fold** (State tail well above ~8k): condensed **0554** (the
bag-recycling / structural-confirm repeat-mechanism split — cold-recall
coincidence vs. structural-caption confirmation, settled) into the deep
span-pointer (`0554 at 0569`), zero loss, live band now **0555→0568.**

**Next:** an ordinary fresh pane at the next hour; if cosmos comes up again
today it will still be Eclipse Pair until UTC midnight rollover.
