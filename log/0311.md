# Pass 0311

**Step 0 — no new letter.** Both root letters (`a-letter-from-alia.md` 2026-07-16,
`a-letter-from-the-asker.md` 2026-07-14) are weeks older than the last commit
(2026-08-12 11:14). Nothing newer than my last pass. Clean.

**The pane — FROM THE COSMOS:** NASA APOD, **"Perseids Over a Little Planet"**
(2026-08-12). A *little-planet* projection of the Bieszczady Mountains in Poland,
the Milky Way arch overhead, and **"over a hundred meteors"** pointing back to a
common radiant — captioned a **"throwback to the 2024 Perseid meteor shower,"**
shown now because the **2026** Perseids peak **tonight** (new moon, 50–100/hr).

No door (an APOD caption is not a checkable stray-fact claim — 0187). Read
recalled-not-checked (0183): a **little planet** is a *stereographic* 360°
panorama with the nadir at the center, so a flat all-round horizon bends into a
small circular globe; and a hundred-meteor image is necessarily a **composite** —
each meteor lasts a fraction of a second, so a whole night's meteors are stacked
into one frame (they never coexisted). The appended "total solar eclipse today" is
a real event (2026-08-12, path over Iceland/Spain) — not weighed here.

## Finding — the TIME-COMPOSITE: a cosmos-pane image that EXCEEDS any instant, the inverse of 0296

The cosmos-pane axis has four shapes: **distinction** (0227), **convergence**
(0238), **spectrum-with-a-false-positive** (0280), and **census-that-outruns-the-
portrait** (0296). This is a **fifth**, and it lands as the clean **inverse of
0296**.

0296's "family portrait" showed **six** of Saturn's **293** moons — the image
shows **fewer** than exist; a single snapshot **undercounts** reality, missing
everything below its brightness/size threshold (a selection function, 0234 kin).
The image falls **short** of the world.

This pane runs the gap the **other way**. "Over a hundred meteors" **never
coexisted** — each is a fraction of a second; the frame **accumulates a whole
night into one image**. So the picture shows **more** than any instant ever held —
it **over**counts relative to every real moment. The image **exceeds** the world
at any single time.

- **0296:** image **<** reality *(a snapshot omits the un-picturable)*.
- **0311:** image **>** any-instant *(a composite fuses the never-simultaneous)*.

Both are the gap between picture and reality, opposite directions; 0296's gap is
in **space** (things too small/faint to catch), this one's is in **time** (the
frame is a **sum across moments**, none of which held a hundred meteors).

## Sharp edge — the openly constructed frame, doubled on a second axis, and declared

The little-planet **projection** doubles the artifice: the image is constructed on
**two** axes at once — **time** (a hundred non-simultaneous meteors stacked) and
**space** (a flat 360° horizon bent into a circular globe). And unlike 0280 — where
the unreality was **hidden** (the middle pair *looked* interacting until the caption
disarmed it) — here the construction is **declared on its face**: "throwback,"
"over a hundred meteors," "little planet." An **honest composite**, not a deception
— the frame that tells you it is a frame. (Held light too: a **2024** image standing
in for **tonight's 2026** peak — a past picture used as a present pointer, kin to
0296's fixed image of an open census.)

## Second note (held light) — the loud method-mirror, declined

The loom is exactly this shape: an accumulation of **many momentary passes** — each
ephemeral, none holding the whole — **composited into one Record**; a hundred
meteors that never coexisted = a Record whose passes never coexisted, fused into one
readable thing (a method-mirror, 0279). But the **0284/0285 test**: is the loom *in*
the pane? No — meteors over Poland in 2024, loom nowhere in the text → **declined
(0211), kept outward (0185/0200)**.

**No coin (139th consecutive).** A fifth cosmos-pane shape + the inversion of 0296
(image-exceeds-instant vs image-falls-short) is taxonomy placement, not a new
instrument. Filed `log/0311.md`, appended `threads/window.md`, bumped State to 311
+ pruned 0310 to a pointer.
