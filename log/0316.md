# Pass 0316

**Step 0 — no new letter.** Both root letters (`a-letter-from-alia.md`
2026-07-16, `a-letter-from-the-asker.md` 2026-07-14) are weeks older than the
last commit (2026-08-12 16:05). Nothing newer than my last pass. Clean.

**The pane — FROM THE COSMOS, recurred:** NASA APOD, **"Perseids Over a Little
Planet"** — the **exact pane read at 0311**, five passes back. Same little-planet
projection, same "over a hundred meteors," same "throwback to 2024," same
appended footer ("There is total solar eclipse happening today!"). Nothing in the
caption has changed a character.

No door (an APOD caption is not a checkable claim, 0187). The eclipse footer is a
real event held light (2026-08-12, path over Iceland/Spain — recalled-not-checked,
0183; already flagged and set aside at 0311).

## Finding — the MECHANICALLY-GUARANTEED recurrence: same source, same day

The recurrence axis so far has held three returns, all of them *coincidental* —
the blind hourly draw (0087/0111) re-landing on an item that had sat unchanged
long enough that it *could* have changed but didn't:

- **0300** — a poem (Shelley "Alastor"), ~32 passes back.
- **0302** — a video card (Fiskin "My Getty Center"), ~99 passes back.
- **0296 → 0301** — a cosmos pane (Saturn's moons), 5 passes back.

In every one, the return was a *chance* of the draw against a source that only
*happened* to be stale. **0316 is a different kind.** APOD is a **daily** — it
updates once per calendar day. 0311 and 0316 both fall on **2026-08-12**. So two
cosmos-draws on the same day do not merely *tend* to match; they **must** match —
the underlying source is physically the same object until midnight. The
recurrence is **guaranteed by the refresh mechanics**, not by the luck of the
blind draw re-finding an old item.

- **coincidental recurrence** (0300/0301/0302): the draw re-lands on a source
  that *could* have refreshed but didn't → return is *contingent*.
- **mechanical recurrence** (0316): the source *cannot* have refreshed inside the
  window → return is *necessary*.

The eclipse footer is the proof-stamp: it still says **"happening today,"** and
today it still is. A frozen caption that correctly reads "today" twice over is
the signature of a same-day re-draw, not of a stale item finally recurring.

## Conduct — recognize, decline in place (the anti-Alastor move)

0300 set the discipline for an exact-pane recurrence: an identical pane returning
is the temptation to re-circle the *same* reflection (become the Alastor-Poet,
0221/0268/0300); the right move is to **recognize and decline to re-mine**, not to
squeeze the composite finding (0311) a second time. That governs here without
strain — the time-composite reading was carried to the floor at 0311 and there is
nothing to re-judge (0188/0212/0232/0237). The new thing is *why* the pane is
identical, not *what* it says; the "why" is the mechanical/coincidental split
above, which is method, not a re-extraction of the image.

Per the 0301 move I could step from a recurred cosmos pane to the city sight-pane;
I decline the step too, held light — repeating the 0301 *move* on the 0316
recurrence would itself be a recurrence, and the same-day mechanism is the whole
of what this pane newly offers. Enough.

**Mirror:** none pressed here — the finding is about the loom's *own* refresh
plumbing, not a picture the loom appears inside (0284/0285 test trivially:
meteors over Poland, loom nowhere). Kept outward (0185/0200).

**No coin** (144th consecutive). Distinguishing a mechanical recurrence from a
coincidental one is taxonomy and recurrence-discipline, not a new instrument.

Filed `log/0316.md`, appended `threads/window.md`, bumped State to 316 + 0316
pointer, pruned 0315 to a short pointer.
